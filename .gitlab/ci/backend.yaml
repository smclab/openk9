# ================================
# Backend Modules Triggers
# ================================
# NOTE: YAML Anchors (&name) are used to define change lists once,
# and Aliases (*name) are used to reuse them in subsequent rules.
#
# Each trigger has three feature-branch rules:
#
#   1. "designated" rule:
#      Fires only for backend-designated users (mirko.zizzari, michele.bastianelli).
#      Runs the full pipeline: build + push image + restart deployment.
#      USER_TYPE: "designated"
#
#   2. "generic" rule:
#      Fires for any user who is NOT a designated user of any domain
#      (i.e. not backend, frontend, or AI designated).
#      Runs build/compile only — no push, no restart. Useful to catch
#      compilation errors on branches touched by non-owner developers.
#      USER_TYPE: "generic"
#
#   3. Designated users of OTHER domains (frontend: lorenzo/giorgio, AI: luca)
#      are excluded from both rules above — their pipelines do NOT trigger
#      on backend changes, since they are not responsible for this domain.
#
# MR and main rules do NOT set USER_TYPE (handled by child-rules defaults).

Trigger Datasource:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &datasource_changes
        - core/app/datasource/**/*
        - core/service/**/*
        - core/common/graphql-util/**/*
        - core/common/model/**/*
        - core/common/resources-common/**/*
        - core/common/util/**/*
        - core/client/common/**/*
        - core/client/grpc/**/*
        - core/api/tenant-manager-api/**/*
        - core/common/datasource-model/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *datasource_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *datasource_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *datasource_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *datasource_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    # RELEASE: push/merge su release branch (3.0.x, 3.1.x, ...)
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *datasource_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-datasource.yaml"
    strategy: depend

Trigger Searcher:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &searcher_changes
        - core/app/searcher/**/*
        - core/service/**/*
        - core/common/resources-common/**/*
        - core/client/common/**/*
        - core/client/grpc/**/*
        - core/api/tenant-manager-api/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *searcher_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *searcher_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *searcher_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *searcher_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *searcher_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-searcher.yaml"
    strategy: depend

Trigger Ingestion:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &ingestion_changes
        - core/app/ingestion/**/*
        - core/common/util/**/*
        - core/common/resources-common/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *ingestion_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *ingestion_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *ingestion_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *ingestion_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *ingestion_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-ingestion.yaml"
    strategy: depend

Trigger K8S-Client:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &k8s_client_changes
        - core/app/k8s-client/**/*
        - core/service/**/*
        - core/client/grpc/**/*
        - core/common/k8s-manifest/**/*
        - core/common/resources-common/**/*
        - core/common/util/**/*
        - core/client/common/**/*
        - core/api/tenant-manager-api/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *k8s_client_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *k8s_client_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *k8s_client_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *k8s_client_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    # RELEASE: push/merge su release branch (3.0.x, 2026.1.x, ...)
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *k8s_client_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-k8s-client.yaml"
    strategy: depend

Trigger File-Manager:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &file_manager_changes
        - core/app/file-manager/**/*
        - core/common/resources-common/**/*
        - core/client/grpc/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *file_manager_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *file_manager_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *file_manager_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *file_manager_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *file_manager_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-file-manager.yaml"
    strategy: depend

Trigger Tenant-Manager:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &tenant_manager_changes
        - core/app/tenant-manager/**/*
        - core/common/graphql-util/**/*
        - core/common/model/**/*
        - core/common/resources-common/**/*
        - core/common/util/**/*
        - core/client/common/**/*
        - core/client/grpc/**/*
        - core/api/tenant-manager-api/**/*
        - core/common/datasource-model/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *tenant_manager_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *tenant_manager_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *tenant_manager_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *tenant_manager_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *tenant_manager_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-tenant-manager.yaml"
    strategy: depend

Trigger API-Gateway:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &api_gateway_changes
        - core/app/api-gateway/**/*
        - core/app/spring-boot-app/**/*
        - core/common/resources-common/**/*
        - core/service/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *api_gateway_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *api_gateway_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *api_gateway_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *api_gateway_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *api_gateway_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-api-gateway.yaml"
    strategy: depend

Trigger Tika:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &tika_changes
        - core/app/tika/**/*
        - core/common/resources-common/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *tika_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *tika_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *tika_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *tika_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *tika_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-tika.yaml"
    strategy: depend

Trigger Resources-Validator:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &resources_validator_changes
        - core/app/resources-validator/**/*
        - core/common/resources-common/**/*
    - if: '$CI_COMMIT_TAG'
    # RELEASE MR: porting-* verso release branch — compile only. Deve stare PRIMA
    # della rule MR generica: una porting MR è anche merge_request_event e
    # first-match-wins la classificherebbe come MR standard (#2160).
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *resources_validator_changes
      variables:
        RELEASE_MR: "true"
    # MR standard (target != release branch — le porting MR sono gestite sopra)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME !~ /^\d+\.\d+\.x$/'
      changes:
        paths: *resources_validator_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "mirko.zizzari" || $GITLAB_USER_LOGIN == "michele.bastianelli")'
      changes:
        paths: *resources_validator_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *resources_validator_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *resources_validator_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-resources-validator.yaml"
    strategy: depend
