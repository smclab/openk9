# ================================
# AI & ML Modules Triggers
# ================================
# NOTE: YAML Anchors (&name) are used to define change lists once,
# and Aliases (*name) are used to reuse them in subsequent rules.

Trigger Rag Module:
  stage: trigger
  rules:
    # 1. Main Branch Rule - DEFINES the changes list (&rag_module_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &rag_module_changes
        - ai-packages/rag-module/**/*
    # 2. Tag Rule - REUSES changes list (*rag_module_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *rag_module_changes
    # 3. Feature Branch Rule - REUSES changes list (*rag_module_changes)
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *rag_module_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-rag-module.yaml"
    strategy: depend

Trigger Embedding Modules:
  stage: trigger
  rules:
    # Define changes (&embedding_modules_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &embedding_modules_changes
        - ai-packages/embedding-modules/**/*
    # Reuse changes (*embedding_modules_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *embedding_modules_changes
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *embedding_modules_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-embedding-module.yaml"
    strategy: depend

# Enrichers / Processors
Trigger Docling Processor:
  stage: trigger
  rules:
    # Define changes (&docling_processor_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &docling_processor_changes
        - ai-packages/docling-processor/**/*
    # Reuse changes (*docling_processor_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *docling_processor_changes
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *docling_processor_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-docling-processor.yaml"
    strategy: depend

Trigger Chunk Evaluation Module:
  stage: trigger
  rules:
    # Define changes (&chunk_eval_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &chunk_eval_changes
        - ai-packages/chunk-evaluation-module/**/*
    # Reuse changes (*chunk_eval_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *chunk_eval_changes
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *chunk_eval_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-chunk-evaluation-module.yaml"
    strategy: depend
