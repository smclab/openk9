# ================================
# Frontend Modules Triggers
# ================================
# NOTE: YAML Anchors (&name) are used to define change lists once,
# and Aliases (*name) are used to reuse them in subsequent rules.
#
# Each trigger has three feature-branch rules:
#
#   1. "designated" rule:
#      Fires only for frontend-designated users (lorenzo.venneri, giorgio.bartolomeo).
#      Runs the full pipeline: build + push image + restart deployment.
#      USER_TYPE: "designated"
#
#   2. "generic" rule:
#      Fires for any user who is NOT a designated user of any domain
#      (i.e. not backend, frontend, or AI designated).
#      Runs build/compile only — no push, no restart. Useful to catch
#      compilation errors on branches touched by non-owner developers.
#      USER_TYPE: "generic"
#
#   3. Designated users of OTHER domains (backend: mirko/michele, AI: luca)
#      are excluded from both rules above — their pipelines do NOT trigger
#      on frontend changes, since they are not responsible for this domain.
#
# MR and main rules do NOT set USER_TYPE (handled by child-rules defaults).

Trigger Search Frontend:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &search_frontend_changes
        - js-packages/search-frontend/**/*
    - if: '$CI_COMMIT_TAG'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        paths: *search_frontend_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "lorenzo.venneri" || $GITLAB_USER_LOGIN == "giorgio.bartolomeo")'
      changes:
        paths: *search_frontend_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *search_frontend_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    # RELEASE: push/merge su release branch (3.0.x, 3.1.x, ...)
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *search_frontend_changes
    # RELEASE MR: porting-* verso release branch — compile only
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *search_frontend_changes
      variables:
        RELEASE_MR: "true"
  trigger:
    include: "/.gitlab/.gitlab-ci-search-frontend.yaml"
    strategy: depend

Trigger Admin Frontend:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &admin_ui_changes
        - js-packages/admin-ui/**/*
    - if: '$CI_COMMIT_TAG'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        paths: *admin_ui_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "lorenzo.venneri" || $GITLAB_USER_LOGIN == "giorgio.bartolomeo")'
      changes:
        paths: *admin_ui_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *admin_ui_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *admin_ui_changes
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *admin_ui_changes
      variables:
        RELEASE_MR: "true"
  trigger:
    include: "/.gitlab/.gitlab-ci-admin-frontend.yaml"
    strategy: depend

Trigger Tenant Frontend:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &tenant_ui_changes
        - js-packages/tenant-ui/**/*
    - if: '$CI_COMMIT_TAG'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        paths: *tenant_ui_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "lorenzo.venneri" || $GITLAB_USER_LOGIN == "giorgio.bartolomeo")'
      changes:
        paths: *tenant_ui_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *tenant_ui_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *tenant_ui_changes
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *tenant_ui_changes
      variables:
        RELEASE_MR: "true"
  trigger:
    include: "/.gitlab/.gitlab-ci-tenant-frontend.yaml"
    strategy: depend

Trigger Talk-To Frontend:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &talk_to_changes
        - js-packages/talk-to/**/*
    - if: '$CI_COMMIT_TAG'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        paths: *talk_to_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "lorenzo.venneri" || $GITLAB_USER_LOGIN == "giorgio.bartolomeo")'
      changes:
        paths: *talk_to_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *talk_to_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *talk_to_changes
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *talk_to_changes
      variables:
        RELEASE_MR: "true"
  trigger:
    include: "/.gitlab/.gitlab-ci-talk-to.yaml"
    strategy: depend

Trigger OpenK9-Chatbot:
  stage: trigger
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &chatbot_changes
        - js-packages/openk9-chatbot/**/*
    - if: '$CI_COMMIT_TAG'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        paths: *chatbot_changes
        compare_to: 'refs/heads/main'
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && ($GITLAB_USER_LOGIN == "lorenzo.venneri" || $GITLAB_USER_LOGIN == "giorgio.bartolomeo")'
      changes:
        paths: *chatbot_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "designated"
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/ && $GITLAB_USER_LOGIN != "mirko.zizzari" && $GITLAB_USER_LOGIN != "michele.bastianelli" && $GITLAB_USER_LOGIN != "lorenzo.venneri" && $GITLAB_USER_LOGIN != "giorgio.bartolomeo" && $GITLAB_USER_LOGIN != "luca.callocchia"'
      changes:
        paths: *chatbot_changes
        compare_to: 'refs/heads/main'
      variables:
        USER_TYPE: "generic"
    # RELEASE: push/merge su release branch (3.0.x, 2026.1.x, ...)
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *chatbot_changes
    # RELEASE MR: porting-* verso release branch — compile only
    - if: '$CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^\d+\.\d+\.x$/'
      changes: *chatbot_changes
      variables:
        RELEASE_MR: "true"
  trigger:
    include: "/.gitlab/.gitlab-ci-openk9-chatbot.yaml"
    strategy: depend
