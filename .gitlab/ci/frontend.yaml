# ================================
# Frontend Modules Triggers
# ================================
# NOTE: YAML Anchors (&name) are used to define change lists once,
# and Aliases (*name) are used to reuse them in subsequent rules.

Trigger Search Frontend:
  stage: trigger
  rules:
    # 1. Main Branch Rule - DEFINES the changes list (&search_frontend_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &search_frontend_changes
        - js-packages/search-frontend/**/*
    # 2. Tag Rule - REUSES changes list (*search_frontend_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *search_frontend_changes
    # 3. Feature Branch Rule - REUSES changes list (*search_frontend_changes)
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *search_frontend_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-search-frontend.yaml"
    strategy: depend

Trigger Admin Frontend:
  stage: trigger
  rules:
    # Define changes (&admin_ui_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &admin_ui_changes
        - js-packages/admin-ui/**/*
    # Reuse changes (*admin_ui_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *admin_ui_changes
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *admin_ui_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-admin-frontend.yaml"
    strategy: depend

Trigger Tenant Frontend:
  stage: trigger
  rules:
    # Define changes (&tenant_ui_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &tenant_ui_changes
        - js-packages/tenant-ui/**/*
    # Reuse changes (*tenant_ui_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *tenant_ui_changes
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *tenant_ui_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-tenant-frontend.yaml"
    strategy: depend

Trigger Talk-To Frontend:
  stage: trigger
  rules:
    # Define changes (&talk_to_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &talk_to_changes
        - js-packages/talk-to/**/*
    # Reuse changes (*talk_to_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *talk_to_changes
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *talk_to_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-talk-to.yaml"
    strategy: depend

Trigger OpenK9-Chatbot:
  stage: trigger
  rules:
    # Define changes (&chatbot_changes)
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: &chatbot_changes
        - js-packages/openk9-chatbot/**/*
    # Reuse changes (*chatbot_changes)
    - if: '$CI_COMMIT_TAG'
      changes: *chatbot_changes
    - if: '$CI_COMMIT_BRANCH =~ /^[0-9]+-.*$/'
      changes: *chatbot_changes
  trigger:
    include: "/.gitlab/.gitlab-ci-openk9-chatbot.yaml"
    strategy: depend
