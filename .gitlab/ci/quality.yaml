# ================================
# Quality & Security Checks
# ================================

# Define Anchor for Java files list
.java_files_list: &java_files_list
  - core/**/*
  - vendor/**/*
  - pom.xml

# Define Anchor for all-source files list (Java, JS, Python/AI)
# Used by SonarQube so it triggers on any source change, not just Java.
.all_source_files_list: &all_source_files_list
  - core/**/*
  - vendor/**/*
  - js-packages/**/*
  - ai-packages/**/*
  - enrichers/**/*
  - pom.xml
  - "*.json"
  - "*.js"
  - "*.ts"
  - "*.tsx"

# NB: the old .python_files_list anchor is gone — the Python dep scan is now split
# per-module (see below), each job with a hardcoded changes: path on its own dir.

# Define Anchor for JS/frontend files list (Issue #2004 — Trivy FS JS dep scan)
.js_files_list: &js_files_list
  - js-packages/**/*

# Trivy Ignore Expiry Check (Issue #2004)
# Trivy enforces `expired_at` natively (expired entries are pruned, the CVE
# reappears in the report), but Phase 1 scans are informational and the Phase 2
# gate only fails on CRITICAL/HIGH — an expired MEDIUM would resurface unseen.
# This job reads the 5 area files and fails when any expired_at is in the past
# (or malformed, incl. the misspelled `expired-at` that Trivy silently ignores),
# forcing a re-evaluation. Blocking on main + MR so an expiry is caught before merge.
Trivy Ignore Expiry Check:
  stage: quality
  image: python:3.13-slim
  before_script:
    - pip install --no-cache-dir pyyaml
  script:
    - python .gitlab/trivy-templates/check-expiry.py
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'

# Trivy FS — Java dependency scan (Issue #2004, replaces OWASP dependency-check)
# trivy fs reads the pom.xml tree statically (no `mvnw install`). Covers both
# core/ (the app reactor) and vendor/ (hibernate-rx multitenancy lib). The actual
# JARs in the image layers are covered separately by the per-module .trivy-image-scan.
# Suppressions live in core/.trivyignore (migrated from dependency-check-suppressions.xml).
# Runs where Java deps land: push main, push N.N.x. NOT on MRs (review feedback:
# the dep scan is a report to read on the integration branches, not a per-push
# check on every MR iteration) and NOT on tags (a tag is only a version bump —
# deps are identical to the main/N.N.x commit it points at).
# --offline-scan is REQUIRED here: the core/ reactor leaves most versions to the
# remote quarkus-bom (no inline <version>), so without it trivy fetches hundreds
# of poms from Maven Central on an empty .m2 → HTTP 429 + ~30min runner IP ban.
# Cost: BOM-managed transitives needing a remote pom are skipped — the per-module
# image scan covers the real resolved JARs. Do not drop the flag.
# This job keeps its own script because it scans TWO dirs (core/ + vendor/) and
# `trivy fs` takes exactly ONE target per invocation — passing "core vendor" makes
# it lstat a single literal path and die. It mirrors the .trivy-fs-scan flow
# (2 scans per dir, everything else rendered with `trivy convert`) and now also
# emits the Markdown report the previous 3-loop version skipped. Severity comes
# from TRIVY_SEVERITY_* like everywhere else.
Trivy FS Java:
  extends: .trivy-fs-scan
  variables:
    IGNOREFILE: "core/.trivyignore.yaml"
  script:
    # SCAN A (fixable only) + SCAN B (everything), per dir. Reports are rendered
    # from the SCAN B json — no third scan.
    - for d in core vendor; do
        trivy fs --cache-dir "$TRIVY_CACHE_DIR" --ignorefile "$IGNOREFILE"
          --scanners vuln --offline-scan --ignore-unfixed --severity "$TRIVY_SEVERITY_GATE"
          --format json --output "trivy-deps-java-$d-gate.json" --exit-code 0 "$d";
        trivy fs --cache-dir "$TRIVY_CACHE_DIR" --ignorefile "$IGNOREFILE"
          --scanners vuln --offline-scan --severity "$TRIVY_SEVERITY_REPORT"
          --format json --output "trivy-deps-java-$d.json" --exit-code 0 "$d";
        echo "=== trivy fs (table) $d ===";
        trivy convert --scanners vuln --format table "trivy-deps-java-$d.json";
        trivy convert --scanners vuln --format template --template "@.gitlab/trivy-templates/md.tpl"
          --output "trivy-deps-java-$d.md" "trivy-deps-java-$d.json";
      done
    # GATE (Phase 2): fail if core/ or vendor/ has a fixable CRITICAL/HIGH.
    # Both dirs are evaluated before exiting so the log shows every offender.
    - RC=0; for d in core vendor; do
        trivy convert --scanners vuln --severity "$TRIVY_SEVERITY_GATE"
          --exit-code 1 "trivy-deps-java-$d-gate.json" || RC=1;
      done; exit $RC
  rules:
    # Block first execution when branch is just created
    - !reference [.rules:antispam-new-branch, rules]
    # 1. Main
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: *java_files_list
    # 3. Push on release branch N.N.x
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *java_files_list
    # NB: no tag rule (deps identical to main/N.N.x) and no porting-MR rule
    # (porting → N.N.x stays compile-only — see PLAN §5.5 context 6).

# SonarQube Check
SonarQube Check:
  stage: quality
  image:
    name: sonarsource/sonar-scanner-cli:5.0.1
    entrypoint: [ "" ]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"
    GIT_DEPTH: "0"
  cache:
    key: "sonar-cache"
    paths:
    - .sonar/cache
  before_script:
  - mkdir /tmp/empty
  script:
  - sonar-scanner -Dsonar.qualitygate.wait=true
  allow_failure: true
  rules:
  # Block first execution when branch is just created
  - !reference [.rules:antispam-new-branch, rules]
  # 1. Main Branch - trigger on any source change
  - if: '$CI_COMMIT_BRANCH == "main"'
    changes: *all_source_files_list
  # 2. Merge Request - trigger on any source change
  - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    changes: *all_source_files_list
  # 3. Tag
  - if: "$CI_COMMIT_TAG"

# ============================================================================
# Trivy FS — Python dependency scan (Issue #2004, replaces pip-audit)
# PER-MODULE JOBS, AREA-LEVEL SUPPRESSIONS: one job per Python module (each scans
# its OWN dir, so a CVE surfaces attributed to the right module and only when that
# module's files change), but all AI modules share a single area IGNOREFILE
# (ai-packages/.trivyignore). Suppressions are therefore area-wide — a CVE
# suppressed for one AI module is suppressed for all — a deliberate trade-off for
# fewer files to maintain; keep the affected module in the rationale for traceability.
# Java stays a single reactor job (deps shared by the parent pom) with core/.trivyignore;
# JS stays a single job (frontends share the root yarn.lock) with js-packages/.trivyignore.
#
# Each job:
#   - extends .trivy-fs-scan (2 scans → convert to table/md → gate on the
#     fixable-only json; --offline-scan, severities from TRIVY_SEVERITY_*);
#   - sets SCAN_DIR to its module dir; IGNOREFILE points at the area file;
#   - only runs when files under its own dir change (so a red job names the
#     module that actually regressed).
# The .trivy-fs-scan template writes trivy-deps-$CI_JOB_NAME_SLUG.json, so the
# per-job artifact names never collide.
#
# Same context set as Trivy FS Java (push main / push N.N.x; no MR, no tag, no
# porting-MR). NB: GitLab does NOT expand variables inside rules:changes: paths,
# so each job hardcodes its own dir there (the antispam-new-branch reference and
# the two if: conditions are identical across jobs; only the changes: path and
# the SCAN_DIR variable differ per module — IGNOREFILE is the shared area file).
# ============================================================================

# --- AI modules (ai-packages/) ---
Trivy FS rag-module:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "ai-packages/rag-module"
    IGNOREFILE: "ai-packages/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "ai-packages/rag-module/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "ai-packages/rag-module/**/*" ]

Trivy FS agentic-rag-module:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "ai-packages/agentic-rag-module"
    IGNOREFILE: "ai-packages/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "ai-packages/agentic-rag-module/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "ai-packages/agentic-rag-module/**/*" ]

Trivy FS chunk-evaluation-module:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "ai-packages/chunk-evaluation-module"
    IGNOREFILE: "ai-packages/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "ai-packages/chunk-evaluation-module/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "ai-packages/chunk-evaluation-module/**/*" ]

Trivy FS embedding-modules:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "ai-packages/embedding-modules"
    IGNOREFILE: "ai-packages/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "ai-packages/embedding-modules/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "ai-packages/embedding-modules/**/*" ]

# --- Enrichers (enrichers/) ---
Trivy FS docling-processor:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "enrichers/docling-processor/enricher"
    IGNOREFILE: "enrichers/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "enrichers/docling-processor/enricher/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "enrichers/docling-processor/enricher/**/*" ]

# --- Connectors (connectors/*/connector/) ---
Trivy FS database-connector:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "connectors/database-connector/connector"
    IGNOREFILE: "connectors/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "connectors/database-connector/connector/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "connectors/database-connector/connector/**/*" ]

Trivy FS email-connector:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "connectors/email-connector/connector"
    IGNOREFILE: "connectors/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "connectors/email-connector/connector/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "connectors/email-connector/connector/**/*" ]

Trivy FS gitlab-connector:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "connectors/gitlab-connector/connector"
    IGNOREFILE: "connectors/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "connectors/gitlab-connector/connector/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "connectors/gitlab-connector/connector/**/*" ]

Trivy FS minio-connector:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "connectors/minio-connector/connector"
    IGNOREFILE: "connectors/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "connectors/minio-connector/connector/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "connectors/minio-connector/connector/**/*" ]

Trivy FS openk9-crawler:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "connectors/openk9-crawler/connector"
    IGNOREFILE: "connectors/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "connectors/openk9-crawler/connector/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "connectors/openk9-crawler/connector/**/*" ]

Trivy FS rest-api-connector:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "connectors/rest-api-connector/connector"
    IGNOREFILE: "connectors/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "connectors/rest-api-connector/connector/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "connectors/rest-api-connector/connector/**/*" ]

Trivy FS youtube-connector:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "connectors/youtube-connector/connector"
    IGNOREFILE: "connectors/.trivyignore.yaml"
  rules:
    - !reference [.rules:antispam-new-branch, rules]
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: [ "connectors/youtube-connector/connector/**/*" ]
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: [ "connectors/youtube-connector/connector/**/*" ]

# Trivy FS — JS/frontend dependency scan (Issue #2004, replaces npm-audit analyzer)
# Reads yarn.lock statically — no `yarn install`, so the npm-audit segfault on the
# workspace root (the reason for the old DS_PROJECT_DIR workaround) is gone.
# One job scans the whole js-packages/ tree (root yarn.lock covers admin-ui/
# search-frontend/tenant-ui; talk-to has its own lockfile; chatbot via package.json).
# Moved from the frontend child pipelines to the parent, consistent with Java/Python.
Trivy FS JS:
  extends: .trivy-fs-scan
  variables:
    SCAN_DIR: "js-packages"
    # Single JS ignore file: the frontends share the root yarn.lock, so this one
    # job scans the whole tree and this one file backs it (per-component split is
    # not possible here — see js-packages/.trivyignore header).
    IGNOREFILE: "js-packages/.trivyignore.yaml"
  rules:
    # Block on new branch creation
    - !reference [.rules:antispam-new-branch, rules]
    # 1. Main
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes: *js_files_list
    # 3. Push on release branch N.N.x
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      changes: *js_files_list
