## Summary
(Briefly describe what this merge request does and why. 
Link the related issue if available.
)

## Expected Behavior
(What we expect to happen as a result of these changes. 
The reviewer should be able to verify the changes against this description.
)

## What to Look For
(Help the reviewer focus. 
What conventions might you have missed? 
What are you unsure about?
*If parts of the code were produced or suggested by a coding agent, 
state it here explicitly.*
List any specific concerns: 
 - "Not sure if the React state management is idiomatic" 
 - "Check if the Helm values structure is correct for our deployment pipeline"
 - "Not sure about this annotation used here" 
)

## Dependencies
(List any merge requests that must be merged before this one, 
or that depend on this one.
Example: "This MR depends on !142 (frontend auth changes) being merged into the feature
branch first" 
)

## Test Cases
(In this section, we can list the test cases to validate that the changes 
do not introduce regressions and behave as expected.
We can specify a target environment where we can execute the test cases.)
