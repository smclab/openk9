stages:
- push

push-file-manager-helm:
  stage: push
  image: alpine:3.19
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      when: manual
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/'
      when: manual
  before_script:
  - apk update
  - apk add --update curl helm && rm -rf /var/cache/apk/*
  script:
  - cd helm-charts/02-file-handling && mkdir package
  - |
    if [ -n "$CI_COMMIT_TAG" ]; then
      helm package openk9-file-manager --version "${CI_COMMIT_TAG#v}" -d package
    else
      helm package openk9-file-manager -d package
    fi
  - curl -u $HELM_REPO_USERNAME:$HELM_REPO_PWD $HELM_REPO_URL --upload-file package/*.tgz -v

push-tika-helm:
  stage: push
  image: alpine:3.19
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      when: manual
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/'
      when: manual
  before_script:
  - apk update
  - apk add --update curl helm && rm -rf /var/cache/apk/*
  script:
  - cd helm-charts/02-file-handling && mkdir package
  - |
    if [ -n "$CI_COMMIT_TAG" ]; then
      helm package openk9-tika --version "${CI_COMMIT_TAG#v}" -d package
    else
      helm package openk9-tika -d package
    fi
  - curl -u $HELM_REPO_USERNAME:$HELM_REPO_PWD $HELM_REPO_URL --upload-file package/*.tgz -v

push-resources-validator-helm:
  stage: push
  image: alpine:3.19
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      when: manual
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/'
      when: manual
  before_script:
  - apk update
  - apk add --update curl helm && rm -rf /var/cache/apk/*
  script:
  - cd helm-charts/02-file-handling && mkdir package
  - |
    if [ -n "$CI_COMMIT_TAG" ]; then
      helm package openk9-resources-validator --version "${CI_COMMIT_TAG#v}" -d package
    else
      helm package openk9-resources-validator -d package
    fi
  - curl -u $HELM_REPO_USERNAME:$HELM_REPO_PWD $HELM_REPO_URL --upload-file package/*.tgz -v
