stages:
  - push

push-openk9-rag-module-helm:
  stage: push
  image: alpine:3.19
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      when: manual
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+/'
      when: manual
  before_script:
    - apk update
    - apk add --update curl helm && rm -rf /var/cache/apk/*
  script:
    - cd helm-charts/03-gen-ai && mkdir package
    - |
      if [ -n "$CI_COMMIT_TAG" ]; then
        helm package openk9-rag-module --version "$CI_COMMIT_TAG" -d package
      else
        helm package openk9-rag-module -d package
      fi
    - curl -u $HELM_REPO_USERNAME:$HELM_REPO_PWD $HELM_REPO_URL --upload-file package/*.tgz -v

push-openk9-embedding-module-helm:
  stage: push
  image: alpine:3.19
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      when: manual
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+/'
      when: manual
  before_script:
    - apk update
    - apk add --update curl helm && rm -rf /var/cache/apk/*
  script:
    - cd helm-charts/03-gen-ai && mkdir package
    - |
      if [ -n "$CI_COMMIT_TAG" ]; then
        helm package openk9-embedding-module --version "$CI_COMMIT_TAG" -d package
      else
        helm package openk9-embedding-module -d package
      fi
    - curl -u $HELM_REPO_USERNAME:$HELM_REPO_PWD $HELM_REPO_URL --upload-file package/*.tgz -v

push-talk-to-helm:
  stage: push
  image: alpine:3.19
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
    - if: '$CI_COMMIT_BRANCH =~ /^\d+\.\d+\.x$/'
      when: manual
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+/'
      when: manual
  before_script:
    - apk update
    - apk add --update curl helm && rm -rf /var/cache/apk/*
  script:
    - cd helm-charts/03-gen-ai && mkdir package
    - |
      if [ -n "$CI_COMMIT_TAG" ]; then
        helm package openk9-talk-to --version "$CI_COMMIT_TAG" -d package
      else
        helm package openk9-talk-to -d package
      fi
    - curl -u $HELM_REPO_USERNAME:$HELM_REPO_PWD $HELM_REPO_URL --upload-file package/*.tgz -v

