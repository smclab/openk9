###################### INGESTION CHILD PIPELINE ######################
include:
  - local: '/.gitlab/.gitlab-templates.yaml'
  - local: '/.gitlab/ci/child-rules.yaml'

stages:
- build
- build-verifier
- container-scanning
- restart
- push

Build Ingestion image:
  extends: .build_template
  variables:
    COMPONENT: "ingestion"
  stage: build
  script:
  - cd core
  - !reference [.maven-build-logic, script]
  artifacts:
    paths:
    - .version
    expire_in: 24 hours
  rules:
    # main/tag: push versione; designated feature: push snapshot; generic feature: build no push
    - !reference [.rules:child-deploy-main-tag, rules]
    - !reference [.rules:child-restart-feature, rules]
    - !reference [.rules:child-build-feature-generic, rules]

######################## BUILD VERIFIER ########################
Build Verifier Ingestion:
  extends: .maven-verifier-template
  stage: build-verifier
  variables:
    COMPONENT: "ingestion"
  rules:
    # Run only on MR - lightweight compile check, no push
    - !reference [.rules:child-verify-mr, rules]

# SCANNING JOB
Container Scanning Ingestion:
  dependencies: [ Build Ingestion image ]
  extends: .container-scanning-template
  variables:
    REGISTRY_COMPONENT_NAME: "openk9-ingestion"
  rules:
    - !reference [.rules:child-deploy-main-tag, rules]

# RESTART JOB
Trigger Restart Ingestion:
  extends: .restart_job_template
  dependencies:
  - Build Ingestion image
  variables:
    COMPONENT_NAME: "ingestion"
    COMPONENT_TYPE: "backend"
  rules:
    - !reference [.rules:child-restart-main, rules]
    - !reference [.rules:child-restart-feature, rules]

# --- RELEASE BRANCH JOBS ---

Build Ingestion Release:
  extends: .build_template
  variables:
    COMPONENT: "ingestion"
  stage: build
  script:
  - cd core
  - !reference [.maven-build-logic-release, script]
  artifacts:
    paths:
    - .version
    expire_in: 24 hours
  rules:
    - !reference [.rules:child-deploy-release, rules]

Build Verifier Ingestion Release MR:
  extends: .maven-verifier-template
  stage: build-verifier
  variables:
    COMPONENT: "ingestion"
  script:
  - !reference [.maven-build-verifier-logic, script]
  rules:
    - !reference [.rules:child-verify-release-mr, rules]

Trigger Restart Ingestion Release:
  extends: .restart_job_template
  dependencies:
  - Build Ingestion Release
  variables:
    COMPONENT_NAME: "ingestion"
    COMPONENT_TYPE: "backend"
    TARGET_ENV: "$RELEASE_TARGET_ENV"
  rules:
    - !reference [.rules:child-restart-release, rules]

######################## COPY TO DOCKER HUB ########################
# Runs only on Git tag. Copies the already-built image to Docker Hub without rebuilding.
# See .skopeo-copy-to-dockerhub in .gitlab-templates.yaml for full details.
Copy Ingestion to DockerHub:
  extends: .skopeo-copy-to-dockerhub
  variables:
    REGISTRY_COMPONENT_NAME: "openk9-ingestion"
  needs:
    - job: Build Ingestion Release
      artifacts: true  # downloads .version file — needed to resolve the image tag
