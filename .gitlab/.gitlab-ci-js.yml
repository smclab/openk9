variables:
  YARN_CACHE_FOLDER: ../.yarn-cache

cache:
  paths:
    - .yarn-cache/

Build Tagged JS Docker Images:
  image: docker:19.03.13
  services:
    - docker:19.03.13-dind
  stage: build-docker
  variables:
    TEST_NAME: local/test
    DOCKER_HOST: tcp://localhost:2375
    DOCKER_TLS_CERTDIR: ""
  only:
    - tags
  before_script:
    - docker login -u $CI_REGISTRY_USERNAME -p $CI_REGISTRY_PASSWORD $CI_REGISTRY_NAME
  script:
    - docker build -t search-admin:${CI_COMMIT_REF_NAME} -f js-packages/admin-ui/Dockerfile .
    - docker tag search-admin:${CI_COMMIT_REF_NAME} $CI_REGISTRY_NAME/openk9/search-admin:${CI_COMMIT_REF_NAME}
    - docker push $CI_REGISTRY_NAME/openk9/search-admin:${CI_COMMIT_REF_NAME}
  allow_failure: false

Build Tagged Frontend Docker Image:
  image: docker:19.03.13
  services:
    - docker:19.03.13-dind
  stage: build-docker
  variables:
    TEST_NAME: local/test
    DOCKER_HOST: tcp://localhost:2375
    DOCKER_TLS_CERTDIR: ""
  only:
    - tags
  before_script:
    - docker login -u $CI_REGISTRY_USERNAME -p $CI_REGISTRY_PASSWORD $CI_REGISTRY_NAME
  script:
    - docker build -t search-frontend:${CI_COMMIT_REF_NAME} -f js-packages/search-frontend/Dockerfile .
    - docker tag search-frontend:${CI_COMMIT_REF_NAME} $CI_REGISTRY_NAME/openk9/search-frontend:${CI_COMMIT_REF_NAME}
    - docker push $CI_REGISTRY_NAME/openk9/search-frontend:${CI_COMMIT_REF_NAME}
  allow_failure: false

Build Query Understanding Frontend Docker Image:
  image: docker:19.03.13
  services:
    - docker:19.03.13-dind
  stage: build-docker
  variables:
    TEST_NAME: local/test
    DOCKER_HOST: tcp://localhost:2375
    DOCKER_TLS_CERTDIR: ""
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - js-packages/rest-api/**/*
        - js-packages/search-ui-components/**/*
        - js-packages/plugin-scripts/**/*
        - js-packages/search-customizable-ui/**/*
        - js-packages/search-frontend/**/*
  except:
    - schedules
  before_script:
    - docker login -u $CI_REGISTRY_USERNAME -p $CI_REGISTRY_PASSWORD $CI_REGISTRY_NAME
    - apk add --update curl && rm -rf /var/cache/apk/*
  script:
    - docker build -t search-frontend:latest -f js-packages/search-frontend/Dockerfile .
    - docker tag search-frontend:latest $CI_REGISTRY_NAME/openk9/search-frontend:latest
    - docker push $CI_REGISTRY_NAME/openk9/search-frontend:latest
  after_script:
    - curl -X POST -F token=$DEV_TOKEN -F "variables[TRIGGER_JOB]=deploy-query-frontend" -F ref=master $DEV_TRIGGER_URL
  allow_failure: false

Build Latest Site Docker Image:
  image: docker:19.03.13
  services:
    - docker:19.03.13-dind
  stage: build-docker
  variables:
    TEST_NAME: local/test
    DOCKER_HOST: tcp://localhost:2375
    DOCKER_TLS_CERTDIR: ""
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - js-packages/docs-website/**/*
  except:
    - schedules
  before_script:
    - docker login -u $CI_REGISTRY_USERNAME -p $CI_REGISTRY_PASSWORD $CI_REGISTRY_NAME
    - apk add --update curl && rm -rf /var/cache/apk/*
  script:
    - docker build -t docs-website:latest -f js-packages/docs-website/Dockerfile .
    - docker tag docs-website:latest $CI_REGISTRY_NAME/openk9/docs-website:latest
    - docker push $CI_REGISTRY_NAME/openk9/docs-website:latest
  allow_failure: false

Build Latest Admin Ui Docker Image:
  image: docker:19.03.13
  services:
    - docker:19.03.13-dind
  stage: build-docker
  variables:
    TEST_NAME: local/test
    DOCKER_HOST: tcp://localhost:2375
    DOCKER_TLS_CERTDIR: ""
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - js-packages/rest-api/**/*
        - js-packages/search-ui-components/**/*
        - js-packages/plugin-scripts/**/*
        - js-packages/admin-ui/**/*
  except:
    - schedules
  before_script:
    - docker login -u $CI_REGISTRY_USERNAME -p $CI_REGISTRY_PASSWORD $CI_REGISTRY_NAME
    - apk add --update curl && rm -rf /var/cache/apk/*
  script:
    - docker build -t search-admin:latest -f js-packages/admin-ui/Dockerfile .
    - docker tag search-admin:latest $CI_REGISTRY_NAME/openk9/search-admin:latest
    - docker push $CI_REGISTRY_NAME/openk9/search-admin:latest
  after_script:
    - curl -X POST -F token=$DEV_TOKEN -F "variables[TRIGGER_JOB]=deploy-admin-ui" -F ref=master $DEV_TRIGGER_URL
  allow_failure: false


Build Latest Admin Logs Service Docker Image:
  image: docker:19.03.13
  services:
    - docker:19.03.13-dind
  stage: build-docker
  variables:
    TEST_NAME: local/test
    DOCKER_HOST: tcp://localhost:2375
    DOCKER_TLS_CERTDIR: ""
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - js-packages/admin-logs-service/**/*
  except:
    - schedules
  before_script:
    - docker login -u $CI_REGISTRY_USERNAME -p $CI_REGISTRY_PASSWORD $CI_REGISTRY_NAME
    - apk add --update curl && rm -rf /var/cache/apk/*
  script:
    - docker build -t admin-logs-service:latest -f js-packages/admin-logs-service/Dockerfile .
    - docker tag admin-logs-service:latest $CI_REGISTRY_NAME/openk9/admin-logs-service:latest
    - docker push $CI_REGISTRY_NAME/openk9/admin-logs-service:latest
  after_script:
    - curl -X POST -F token=$DEV_TOKEN -F "variables[TRIGGER_JOB]=deploy-admin-logs-service" -F ref=master $DEV_TRIGGER_URL
  allow_failure: false

Build Latest Js Executor Docker Image:
  image: docker:19.03.13
  services:
    - docker:19.03.13-dind
  stage: build-docker
  variables:
    TEST_NAME: local/test
    DOCKER_HOST: tcp://localhost:2375
    DOCKER_TLS_CERTDIR: ""
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - js-packages/js-scripts-executor/**/*
  except:
    - schedules
  before_script:
    - docker login -u $CI_REGISTRY_USERNAME -p $CI_REGISTRY_PASSWORD $CI_REGISTRY_NAME
    - apk add --update curl && rm -rf /var/cache/apk/*
  script:
    - docker build -t js-scripts-executor:latest -f js-packages/js-scripts-executor/Dockerfile .
    - docker tag js-scripts-executor:latest $CI_REGISTRY_NAME/openk9/js-scripts-executor:latest
    - docker push $CI_REGISTRY_NAME/openk9/js-scripts-executor:latest
  after_script:
    - curl -X POST -F token=$DEV_TOKEN -F "variables[TRIGGER_JOB]=deploy-js" -F ref=master $DEV_TRIGGER_URL
  allow_failure: false

trigger_mvp:
  image: curlimages/curl
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - js-packages/rest-api/**/*
        - js-packages/search-ui-components/**/*
        - js-packages/plugin-scripts/**/*
        - js-packages/search-customizable-ui/**/*
        - js-packages/search-frontend/**/*
  except:
    - schedules
  needs: ['Build Query Understanding Frontend Docker Image']
  before_script:
    - apk add --update curl && rm -rf /var/cache/apk/*
  script:
    - curl -X POST -F token=$MVP_TOKEN -F ref=main $MVP_TRIGGER_URL