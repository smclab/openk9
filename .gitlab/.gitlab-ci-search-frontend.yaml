###################### SEARCH FRONTEND CHILD PIPELINE — RELEASE BRANCH ######################
include:
  - local: "/.gitlab/.gitlab-templates.yaml"
  - local: '/.gitlab/ci/child-rules.yaml'

stages:
- build
- build-verifier
- container-scanning
- restart
- push

#################
# Fetch Version #
#################
Fetch version:
  stage: build
  image: node:22.14.0-alpine
  script:
  - cd js-packages/search-frontend
  - export VERSION=$(node -p "require('./package.json').version")
  - echo "VERSION=$VERSION" > $CI_PROJECT_DIR/version.env
  artifacts:
    paths:
    - version.env
    expire_in: 1 day
  rules:
    - !reference [.rules:child-build-always, rules]

######################## BUILD SEARCH FRONTEND RELEASE (KANIKO) ########################
# Push 3.0.x  → image:<VERSION-from-package.json> (e.g. 3.0.3-SNAPSHOT)
# Tag 3.0.N   → image:<CI_COMMIT_TAG>             (e.g. 3.0.1)
Build Search Frontend Release:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [ "" ]
  variables:
    DOCKER_CONFIG: "/kaniko/.docker"
  needs:
  - job: Fetch version
    artifacts: true
  script:
  - export NODE_OPTIONS="--max-old-space-size=8192"
  - mkdir -p /kaniko/.docker
  - |
    REGISTRY_AUTH=$(printf "%s:%s" "${CI_REGISTRY_USERNAME}" "${CI_REGISTRY_PASSWORD}" | base64 | tr -d '\n')
    DOCKERHUB_AUTH=$(printf "%s:%s" "${DOCKER_HUB_USER}" "${DOCKER_HUB_PASSWORD}" | base64 | tr -d '\n')
    printf '{"auths":{"registry.smc.it:49083":{"auth":"%s"},"https://index.docker.io/v1/":{"auth":"%s"}}}' \
      "${REGISTRY_AUTH}" "${DOCKERHUB_AUTH}" > /kaniko/.docker/config.json
  - |
    if [[ -n "$CI_COMMIT_TAG" ]]; then
      IMAGE_TAG="${CI_COMMIT_TAG#v}"
      KANIKO_PUSH="--destination $CI_REGISTRY_NAME/openk9/openk9-search-frontend:$IMAGE_TAG"
      echo "=== Release tag: push $IMAGE_TAG ==="
    else
      source $CI_PROJECT_DIR/version.env
      KANIKO_PUSH="--destination $CI_REGISTRY_NAME/openk9/openk9-search-frontend:$VERSION"
      echo "=== Release branch: push $VERSION ==="
    fi
  - |
    /kaniko/executor \
      --cache=true \
      --cache-repo=kaniko-cache-registry.openk9.io/kaniko-cache \
      --cache-ttl=72h \
      --snapshot-mode=redo \
      --compressed-caching=false \
      --context "${CI_PROJECT_DIR}" \
      --dockerfile "${CI_PROJECT_DIR}/js-packages/search-frontend/Dockerfile" \
      $KANIKO_PUSH \
      --build-arg NODE_OPTIONS="--max-old-space-size=8192"
  rules:
    - !reference [.rules:child-build-release, rules]

######################## BUILD VERIFIER (KANIKO --no-push) ########################
# Compile-check via Kaniko --no-push on the same Dockerfile as the release image
# build. Avoids the heavy Webpack build on the runner (yarn install + build on
# node:), which exceeded the 2h job timeout. Reuses the shared Kaniko layer cache.
# Runs on MR porting-* → 3.0.x and on push porting-* (compile only, no push).
Build Verifier Search Frontend:
  stage: build-verifier
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [ "" ]
  variables:
    DOCKER_CONFIG: "/kaniko/.docker"
    GENERATE_SOURCEMAP: "false"
  needs:
  - job: Fetch version
    artifacts: false
  script:
  - mkdir -p /kaniko/.docker
  - printf '{}' > /kaniko/.docker/config.json
  - |
    /kaniko/executor \
      --cache=true \
      --cache-repo=kaniko-cache-registry.openk9.io/kaniko-cache \
      --cache-ttl=72h \
      --snapshot-mode=time \
      --compressed-caching=false \
      --context "${CI_PROJECT_DIR}" \
      --dockerfile "${CI_PROJECT_DIR}/js-packages/search-frontend/Dockerfile" \
      --no-push \
      --build-arg NODE_OPTIONS="--max-old-space-size=8192"
  rules:
    - !reference [.rules:child-verify-release-mr, rules]
    - !reference [.rules:child-verify-porting-push, rules]

######################## CONTAINER SCANNING (push 3.0.x only) ########################
Container Scanning Search Frontend:
  extends: .container-scanning-template
  needs:
  - job: Fetch version
    artifacts: true
  - job: Build Search Frontend Release
    artifacts: false
  variables:
    REGISTRY_COMPONENT_NAME: "openk9-search-frontend"
  rules:
    - !reference [.rules:child-restart-release, rules]

######################## TRIGGER RESTART (push 3.0.x only) ########################
Trigger Restart Search Frontend Release:
  extends: .restart_job_template
  dependencies:
  - Fetch version
  variables:
    COMPONENT_NAME: "search-frontend"
    COMPONENT_TYPE: "frontend"
    TARGET_ENV: "k9-stable-2"
  rules:
    - !reference [.rules:child-restart-release, rules]

######################## COPY TO DOCKER HUB (tag only) ########################
Copy Search Frontend to DockerHub:
  extends: .skopeo-copy-to-dockerhub
  variables:
    REGISTRY_COMPONENT_NAME: "openk9-search-frontend"
  needs:
    - job: Fetch version
      artifacts: true
    - job: Build Search Frontend Release
      artifacts: false
  rules:
    - !reference [.rules:child-skopeo-release, rules]
