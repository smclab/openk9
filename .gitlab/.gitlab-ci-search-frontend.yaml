###################### SEARCH FRONTEND PIPELINE ######################
include:
  - local: "/.gitlab/.gitlab-templates.yaml"
  # NEW: Include standardized child rules
  - local: '/.gitlab/ci/child-rules.yaml'

stages:
- build
- build-verifier
- container-scanning
- dependency-check
- restart
- publish

#################
# Fetch Version #
#################
Fetch version:
  stage: build
  image: node:22.14.0-alpine
  script:
  - cd js-packages/search-frontend
  - export VERSION=$(node -p "require('./package.json').version") # Reads version from package.json
  - echo "VERSION=$VERSION" > $CI_PROJECT_DIR/version.env # Saves version to a file
  artifacts:
    paths:
    - version.env
    expire_in: 1 day
  rules:
    # Always run if triggered
    - !reference [.rules:child-build-always, rules]

######################## BUILD SEARCH FRONTEND ########################
Build Search Frontend:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [ "" ]
  variables:
    DOCKER_CONFIG: "/kaniko/.docker"
  needs:
  - job: Fetch version
    artifacts: true
  script:
  - export NODE_OPTIONS="--max-old-space-size=8192"
  - mkdir -p /kaniko/.docker
  - |
    echo "{\"auths\":{\"registry.smc.it:49083\":{\"auth\":\"$(printf "%s:%s" "${CI_REGISTRY_USERNAME}" "${CI_REGISTRY_PASSWORD}" | base64 | tr -d '\n')\"},\"https://index.docker.io/v1/\":{\"auth\":\"$(printf "%s:%s" "${DOCKER_HUB_USER}" "${DOCKER_HUB_PASSWORD}" | base64 | tr -d '\n')\"}}}" \
    > /kaniko/.docker/config.json

  # Conditional logic to determine which tag to use
  - |
    if [[ "$CI_COMMIT_BRANCH" == "main" ]]; then
      # For main, use version from package.json
      source $CI_PROJECT_DIR/version.env
      echo "=== Building Search Frontend with VERSION=$VERSION ==="
      DOCKER_TAG="$VERSION"
    elif [[ "$CI_COMMIT_BRANCH" =~ ^[0-9]+-.*$ ]]; then
      # For feature branch, determine tag based on user
      if [[ "$GITLAB_USER_LOGIN" == "lorenzo.venneri" || "$GITLAB_USER_LOGIN" == "giorgio.bartolomeo" ]]; then 
        DOCKER_TAG="996-SNAPSHOT"
        echo "=== Building Search Frontend with VERSION=$DOCKER_TAG for frontend developer ==="
      else
        DOCKER_TAG="999-SNAPSHOT"
        echo "=== Building Search Frontend with VERSION=$DOCKER_TAG for feature branch ==="
      fi
    fi

  # Run build with the determined tag
  - |
    # ── Kaniko layer cache (added 2026-02) ────────────────────────────────────────
    # --cache=true         abilita il Docker layer cache
    # --cache-repo         registry:2 su MinIO (k9-requirements, bucket: kaniko-cache)
    #                      evita di riempire il registry SMC con layer temporanei
    # --cache-ttl=168h     layer più vecchi di 7 giorni vengono ignorati
    #                      coerente con il MinIO lifecycle TTL sul bucket
    # --snapshot-mode=redo usa mtime invece di sha256 per rilevare cambiamenti
    #                      riduce "Taking snapshot of full filesystem" da ~900s a ~5s
        # --compressed-caching=false evita l'errore OOM (exit code 137) disabilitando
        #                      la compressione in RAM dei layer giganti (es. PyTorch)
    # ──────────────────────────────────────────────────────────────────────────────
    /kaniko/executor \
      --cache=true \
      --cache-repo=kaniko-cache-registry.openk9.io/kaniko-cache \
      --cache-ttl=168h \
      --snapshot-mode=redo \
      --compressed-caching=false \
      --context "${CI_PROJECT_DIR}" \
      --dockerfile "${CI_PROJECT_DIR}/js-packages/search-frontend/Dockerfile" \
      --destination "$CI_REGISTRY_NAME/openk9/openk9-search-frontend:$DOCKER_TAG" \
      --build-arg NODE_OPTIONS="--max-old-space-size=8192"
  rules:
    # Run build only on push (main, tag, feature branch) - NO MR
    - !reference [.rules:child-deploy-main-tag, rules]
    - !reference [.rules:child-restart-feature, rules]

######################## BUILD VERIFIER (NODE) ########################
Build Verifier:
  stage: build-verifier
  image: node:22.14.0
  cache:
    key:
      prefix: "search-frontend"
      files:
        - yarn.lock
    paths:
    - js-packages/search-frontend/node_modules
    - .yarn-cache
    policy: pull-push
  artifacts:
    paths:
    - js-packages/search-frontend/dist/
    expire_in: 1 day
  script:
  - export NODE_OPTIONS="--max-old-space-size=8192"
  # Disable workspace strict root check to ensure Lerna aggregates smoothly
  - yarn config set registry https://registry.npmjs.org/
  - yarn config set network-timeout 600000
  - yarn config set cache-folder .yarn-cache
  # 1. Install all dependencies from the project root (solves workspace-aggregator timeouts)
  - yarn install --prefer-offline --frozen-lockfile
  # 2. Build ONLY the specific component using Lerna scope
  - CI=false npx lerna run build --scope @openk9ui/search-frontend
  rules:
    # Run only on MR
    - !reference [.rules:child-verify-mr, rules]

######################## CONTAINER SCANNING ########################
Container Scanning:
  stage: container-scanning
  image: $CI_TEMPLATE_REGISTRY_HOST/security-products/container-scanning:7
  dependencies:
  - "Fetch version"
  - "Build Search Frontend"
  script:
  - source version.env # Loads version from artifact
  - echo "Scanning image $CI_REGISTRY_NAME/openk9/openk9-search-frontend:$VERSION"
  - gtcs scan --image "$CI_REGISTRY_NAME/openk9/openk9-search-frontend:$VERSION" # Scans the image
  rules:
    # Run only on Main
    - !reference [.rules:child-deploy-main-tag, rules]

######################## TRIGGER RESTART ########################
Trigger Restart:
  extends: .restart_job_template
  dependencies:
  - Fetch version
  variables:
    COMPONENT_NAME: "search-frontend"
    COMPONENT_TYPE: "frontend"
  rules:
    # Run on Main/Tag and Feature
    - !reference [.rules:child-deploy-main-tag, rules]
    - !reference [.rules:child-restart-feature, rules]

####################### PUBLISH SEARCH FRONTEND #######################
Publish Search Frontend:
  stage: publish
  image: node:22.14.0
  needs:
  - job: Fetch version
    artifacts: true
  script:
  - cd js-packages/search-frontend
  - export NODE_OPTIONS="--max-old-space-size=8192"
  - yarn config set registry https://registry.npmjs.org/
  - yarn config set network-timeout 600000
  - yarn config set cache-folder ../../.yarn-cache
  - yarn install --prefer-offline --frozen-lockfile
  - yarn build
  - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" >> ~/.npmrc
  - npm publish --access public
  rules:
    # Publish only on Main/Tag
    - !reference [.rules:child-publish, rules]
    # Note: Manual rule removed to standard automation, or re-add if strict manual is required.
    # Original file had partial Manual. Moving to automated publish if pipeline succeeds on Main.

####################### DEPENDENCY CHECK #######################
Dependency Check Search Frontend:
  stage: dependency-check
  needs:
  - job: Fetch version
  - job: Build Verifier
    optional: true
  extends: .dependency_check_frontend_template
  allow_failure: true
  variables:
    # Point the npm-audit analyzer to the specific subdirectory.
    # Without this, it finds the root yarn.lock and installs the entire
    # workspace → segfault in yarn 1.22.5 inside the analyzer container.
    DS_PROJECT_DIR: "js-packages/search-frontend"
  script:
  - yarn config set registry https://registry.npmjs.org/
  - yarn config set network-timeout 600000
  - /analyzer run
