stages:
  - quality
  - build
  - test
  - build-docker
  - deploy

include:
  - local: '/gitlab/.gitlab-ci-js.yml'
  - local: '/gitlab/.gitlab-ci-parser.yml'
  - local: '/gitlab/.gitlab-ci-osgi.yml'

trigger_deploy:
  stage: deploy
  script:
    - "curl -X POST -F token=TOKEN -F ref=main https://git.smc.it/api/v4/projects/1136/trigger/pipeline"