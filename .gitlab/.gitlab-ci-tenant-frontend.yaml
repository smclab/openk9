###################### TENANT FRONTEND PIPELINE ######################
include:
  - local: "/.gitlab/.gitlab-templates.yaml"
  # NEW: Include standardized child rules
  - local: '/.gitlab/ci/child-rules.yaml'

stages:
- build
- build-verifier
- container-scanning
- dependency-check
- restart
- push
- publish

#################
# Fetch Version #
#################
Fetch version:
  stage: build
  image: node:22.14.0-alpine
  script:
  - cd js-packages/tenant-ui
  - export VERSION=$(node -p "require('./package.json').version")
  - echo "VERSION=$VERSION" > $CI_PROJECT_DIR/version.env
  artifacts:
    paths:
    - version.env
    expire_in: 1 day
  rules:
    # Always run if triggered
    - !reference [.rules:child-build-always, rules]

######################## BUILD TENANT FRONTEND ########################
Build Tenant Frontend:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [ "" ]
  variables:
    DOCKER_CONFIG: "/kaniko/.docker"
    # GENERATE_SOURCEMAP=false: skips source map generation during production build.
    # Source maps are large files webpack must compute — not useful inside a Docker image.
    # This alone can cut craco/webpack build time by 30-50%.
    GENERATE_SOURCEMAP: "false"
  needs:
  - job: Fetch version
    artifacts: true
  script:
  # Docker credentials configuration
  - export NODE_OPTIONS="--max-old-space-size=8192"
  - mkdir -p /kaniko/.docker
  - |
    REGISTRY_AUTH=$(printf "%s:%s" "${CI_REGISTRY_USERNAME}" "${CI_REGISTRY_PASSWORD}" | base64 | tr -d '\n')
    DOCKERHUB_AUTH=$(printf "%s:%s" "${DOCKER_HUB_USER}" "${DOCKER_HUB_PASSWORD}" | base64 | tr -d '\n')
    printf '{"auths":{"registry.smc.it:49083":{"auth":"%s"},"https://index.docker.io/v1/":{"auth":"%s"}}}' \
      "${REGISTRY_AUTH}" "${DOCKERHUB_AUTH}" > /kaniko/.docker/config.json

  # Conditional logic to determine push behaviour
  - |
    if [[ "$CI_COMMIT_BRANCH" == "main" || -n "$CI_COMMIT_TAG" ]]; then
      source $CI_PROJECT_DIR/version.env
      KANIKO_PUSH="--destination $CI_REGISTRY_NAME/openk9/openk9-tenant-ui:$VERSION"
      echo "=== Building Tenant Frontend with VERSION=$VERSION ==="
    elif [[ "$CI_COMMIT_BRANCH" =~ ^[0-9]+-.*$ ]]; then
      if [[ "$GITLAB_USER_LOGIN" == "lorenzo.venneri" || "$GITLAB_USER_LOGIN" == "giorgio.bartolomeo" ]]; then
        KANIKO_PUSH="--destination $CI_REGISTRY_NAME/openk9/openk9-tenant-ui:996-SNAPSHOT"
        echo "=== Building Tenant Frontend with VERSION=996-SNAPSHOT for frontend developer ==="
      else
        KANIKO_PUSH="--no-push"
        echo "=== Building Tenant Frontend (no push) for generic user ==="
      fi
    fi

  # Run build with the determined push behaviour
  - |
    /kaniko/executor \
      --cache=true \
      --cache-repo=kaniko-cache-registry.openk9.io/kaniko-cache \
      --cache-ttl=72h \
      --snapshot-mode=redo \
      --compressed-caching=false \
      --context "${CI_PROJECT_DIR}" \
      --dockerfile "${CI_PROJECT_DIR}/js-packages/tenant-ui/Dockerfile" \
      $KANIKO_PUSH \
      --build-arg NODE_OPTIONS="--max-old-space-size=8192"
  rules:
    # main/tag: push versione; designated feature: push 996-SNAPSHOT; generic feature: build no push
    - !reference [.rules:child-deploy-main-tag, rules]
    - !reference [.rules:child-restart-feature, rules]
    - !reference [.rules:child-build-feature-generic, rules]

######################## BUILD VERIFIER ########################
Build Verifier:
  stage: build-verifier
  image: node:22.14.0
  cache:
    - key:
        prefix: "shared-yarn"
        files:
          - yarn.lock
      paths:
        - .yarn-cache
      policy: pull-push
    - key:
        prefix: "tenant-ui-nm"
        files:
          - js-packages/tenant-ui/package.json
      paths:
        - js-packages/tenant-ui/node_modules
      policy: pull-push
  script:
  - export NODE_OPTIONS="--max-old-space-size=8192"
  # Disable workspace strict root check to ensure Lerna aggregates smoothly
  - yarn config set registry https://registry.npmjs.org/
  - yarn config set network-timeout 600000
  - yarn config set cache-folder .yarn-cache
  # 1. Install all dependencies from the project root (solves workspace-aggregator timeouts)
  - yarn install --prefer-offline --frozen-lockfile
  # 2. Build ONLY the specific component using Lerna scope
  - CI=false npx lerna run build --scope openk9-tenant
  rules:
    # Run only on MR
    - !reference [.rules:child-verify-mr, rules]

######################## CONTAINER SCANNING ########################
Container Scanning:
  stage: container-scanning
  image: $CS_ANALYZER_IMAGE
  allow_failure: true
  dependencies:
  - "Fetch version"
  - "Build Tenant Frontend"
  script:
  - source version.env
  - echo "=== Scanning image $CI_REGISTRY_NAME/openk9/openk9-tenant-ui:$VERSION ==="
  - gtcs scan --image "$CI_REGISTRY_NAME/openk9/openk9-tenant-ui:$VERSION"
  rules:
    # Run only on Main/Tag
    - !reference [.rules:child-deploy-main-tag, rules]

######################## TRIGGER RESTART ########################
Trigger Restart:
  extends: .restart_job_template
  dependencies:
  - Fetch version
  variables:
    COMPONENT_NAME: "tenant-ui"
    COMPONENT_TYPE: "frontend"
  rules:
    # Run on Main/Tag and Feature
    - !reference [.rules:child-deploy-main-tag, rules]
    - !reference [.rules:child-restart-feature, rules]

# --- RELEASE BRANCH JOBS ---

Build Tenant Frontend Release:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [ "" ]
  variables:
    DOCKER_CONFIG: "/kaniko/.docker"
    GENERATE_SOURCEMAP: "false"
  needs:
  - job: Fetch version
    artifacts: true
  script:
  - export NODE_OPTIONS="--max-old-space-size=8192"
  - mkdir -p /kaniko/.docker
  - |
    REGISTRY_AUTH=$(printf "%s:%s" "${CI_REGISTRY_USERNAME}" "${CI_REGISTRY_PASSWORD}" | base64 | tr -d '\n')
    DOCKERHUB_AUTH=$(printf "%s:%s" "${DOCKER_HUB_USER}" "${DOCKER_HUB_PASSWORD}" | base64 | tr -d '\n')
    printf '{"auths":{"registry.smc.it:49083":{"auth":"%s"},"https://index.docker.io/v1/":{"auth":"%s"}}}' \
      "${REGISTRY_AUTH}" "${DOCKERHUB_AUTH}" > /kaniko/.docker/config.json
  - |
    if [[ -n "$CI_COMMIT_TAG" && "$CI_COMMIT_TAG" =~ ^[0-9]+\.[0-9]+\.[0-9]+ ]]; then
      source $CI_PROJECT_DIR/version.env
      KANIKO_PUSH="--destination $CI_REGISTRY_NAME/openk9/openk9-tenant-ui:$VERSION"
      echo "=== Release tag: push $VERSION ==="
    elif [[ "$CI_COMMIT_BRANCH" =~ ^[0-9]+\.[0-9]+\.x$ ]]; then
      source $CI_PROJECT_DIR/version.env
      SNAP_TAG="${VERSION}-SNAPSHOT"
      KANIKO_PUSH="--destination $CI_REGISTRY_NAME/openk9/openk9-tenant-ui:$SNAP_TAG"
      echo "=== Release branch: push $SNAP_TAG ==="
    else
      KANIKO_PUSH="--no-push"
      echo "=== Release MR: build only, no push ==="
    fi
  - |
    /kaniko/executor \
      --cache=true \
      --cache-repo=kaniko-cache-registry.openk9.io/kaniko-cache \
      --cache-ttl=72h \
      --snapshot-mode=redo \
      --compressed-caching=false \
      --context "${CI_PROJECT_DIR}" \
      --dockerfile "${CI_PROJECT_DIR}/js-packages/tenant-ui/Dockerfile" \
      $KANIKO_PUSH \
      --build-arg NODE_OPTIONS="--max-old-space-size=8192"
  rules:
    - !reference [.rules:child-deploy-release, rules]

Build Verifier Tenant Frontend Release MR:
  stage: build-verifier
  image: node:22.14.0
  cache:
    - key:
        prefix: "shared-yarn"
        files:
          - yarn.lock
      paths:
        - .yarn-cache
      policy: pull
    - key:
        prefix: "tenant-ui-nm"
        files:
          - js-packages/tenant-ui/package.json
      paths:
        - js-packages/tenant-ui/node_modules
      policy: pull
  script:
  - export NODE_OPTIONS="--max-old-space-size=8192"
  - yarn config set registry https://registry.npmjs.org/
  - yarn config set network-timeout 600000
  - yarn config set cache-folder .yarn-cache
  - yarn install --prefer-offline --frozen-lockfile
  - CI=false npx lerna run build --scope openk9-tenant
  rules:
    - !reference [.rules:child-verify-release-mr, rules]

Trigger Restart Tenant Frontend Release:
  extends: .restart_job_template
  dependencies:
  - Fetch version
  variables:
    COMPONENT_NAME: "tenant-ui"
    COMPONENT_TYPE: "frontend"
    TARGET_ENV: "k9-stable-2"
  rules:
    - !reference [.rules:child-deploy-release, rules]

####################### DEPENDENCY CHECK #######################
Dependency Check Tenant UI:
  stage: dependency-check
  needs:
  - job: Fetch version
  - job: Build Verifier
    optional: true
  extends: .dependency_check_frontend_template
  allow_failure: true
  variables:
    # Point the npm-audit analyzer to the specific subdirectory.
    # Without this, it finds the root yarn.lock and installs the entire
    # workspace → segfault in yarn 1.22.5 inside the analyzer container.
    DS_PROJECT_DIR: "js-packages/tenant-ui"
  script:
  - yarn config set registry https://registry.npmjs.org/
  - yarn config set network-timeout 600000
  - /analyzer run

######################## COPY TO DOCKER HUB ########################
# Runs only on Git tag. Copies the already-built image to Docker Hub without rebuilding.
# See .skopeo-copy-to-dockerhub-frontend in .gitlab-templates.yaml for full details.
Copy Tenant UI to DockerHub:
  extends: .skopeo-copy-to-dockerhub-frontend
  variables:
    REGISTRY_COMPONENT_NAME: "openk9-tenant-ui"
  needs:
    - job: Fetch version
      artifacts: true  # downloads version.env — needed to resolve the image version
    - job: Build Tenant Frontend
      artifacts: false  # image is on the registry, no artifacts needed from this job
